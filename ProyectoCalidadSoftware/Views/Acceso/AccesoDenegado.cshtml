@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 60vh;">
    <div class="card shadow-sm border-0 p-4" style="max-width: 500px; width: 100%; background-color: #fff;">
        <div class="card-body text-center">
            <h2 class="text-danger mb-4"><strong>¡Acceso denegado!</strong></h2>
            <p class="lead mb-4">No tienes permisos suficientes para acceder a esta página. Si consideras que esto es un error, contacta con el administrador.</p>

            <a href="@Url.Action("Inicio", "Home")" class="btn btn-primary btn-lg">Volver al inicio</a>
        </div>
    </div>
</div>

<style>
    .d-flex {
        display: flex;
    }

    .justify-content-center {
        justify-content: center;
    }

    .align-items-center {
        align-items: center;
    }

    .card {
        background-color: #fff;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
    }
</style>
